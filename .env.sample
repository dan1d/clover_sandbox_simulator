# Clover Sandbox Simulator Configuration
# =======================================
#
# This file contains global settings. Merchant-specific settings are in .env.json
# Copy this to .env and customize as needed.

# ============================================================
# PLATFORM API (Optional - only needed for non-card operations)
# ============================================================
# OAuth token expires every 30 minutes - refresh via OAuth flow
# Leave commented if only using Ecommerce API for card payments
# CLOVER_MERCHANT_ID=your_merchant_id
# CLOVER_API_TOKEN=your_oauth_token

# Platform API environment
CLOVER_ENVIRONMENT=https://sandbox.dev.clover.com/

# ============================================================
# ECOMMERCE API (Recommended - tokens don't expire)
# ============================================================
# Get from: Merchant Dashboard > Setup > Ecommerce API Tokens
# These enable card payments and proper refunds
# Individual merchant tokens should be in .env.json

# Ecommerce API environments (sandbox)
ECOMMERCE_ENVIRONMENT=https://scl-sandbox.dev.clover.com/
TOKENIZER_ENVIRONMENT=https://token-sandbox.dev.clover.com/

# ============================================================
# OAUTH CONFIGURATION (for token refresh)
# ============================================================
# Get from: Developer Dashboard > Your App > App Settings
# CLOVER_APP_ID=your_app_id
# CLOVER_APP_SECRET=your_app_secret

# ============================================================
# OPTIONAL SETTINGS
# ============================================================

# Logging level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Business type (restaurant, retail, salon)
BUSINESS_TYPE=restaurant

# Tax rate (default 8.25%)
TAX_RATE=8.25
